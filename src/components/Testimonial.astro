---
export interface Props {
	title: string
	body: string
	author: string
	location: string
}

const { title, body, author, location } = Astro.props
---

<article class="flex w-full flex-col gap-y-3 rounded-xl bg-white p-8">
	<div>
		<img
			src="/speech.svg"
			alt=""
		/>
	</div>
	<h3 class="text-2xl font-normal">{title}</h3>
	<p class="line-clamp-6 text-lg text-gray-600">{body}</p>
	<div class="font-bold text-dark-blue">{author} — {location}</div>
</article>
